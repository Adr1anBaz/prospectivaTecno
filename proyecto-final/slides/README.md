# Blu — Slides

## Ejecutar

```bash
npm install
npm run dev
```

Abrir `http://localhost:5173` en el navegador.

## Navegación

- `←` `→` para cambiar slides
- `Espacio` para avanzar
- Click en los dots inferiores

## Build para producción

```bash
npm run build
npm run preview
```
